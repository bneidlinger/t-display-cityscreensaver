#pragma once
#include <Arduino.h>

// Common TTGO T-Display style pins (adjust if your buttons don’t work)
static constexpr int PIN_BTN_LEFT  = 0;   // often BOOT
static constexpr int PIN_BTN_RIGHT = 35;  // input-only on many boards
